const add = (first, second) =>{
    return first + second;
}
const multiply = (first, second) =>{
    return first * second;
}
const div = (first, second) =>{
    return first / second;
}

// .........ekta value pathate hole....
// export default add;

//--------- Multiple value pathate-------------
export {add, multiply, div};